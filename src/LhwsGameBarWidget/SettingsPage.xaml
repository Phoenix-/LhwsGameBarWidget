<Page
    x:Class="LhwsGameBarWidget.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LhwsGameBarWidget"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="12" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock Text="Sensor rows"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center"/>
            <Button x:Name="AddButton" Padding="8,4" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE710;" FontSize="12"/>
                    <TextBlock Text="Add"/>
                </StackPanel>
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Row: bar + two values"
                                        Click="AddBarRow_Click"/>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
        </StackPanel>

        <TextBlock x:Name="ServiceStatusText"
                   Grid.Row="1"
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Opacity="0.6"
                   TextWrapping="Wrap"/>

        <ListView x:Name="RowList"
                  Grid.Row="2"
                  SelectionMode="None"
                  IsItemClickEnabled="False">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0,4"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:RowEditor">
                    <Grid Padding="10"
                          RowSpacing="6"
                          CornerRadius="6"
                          Background="{ThemeResource SystemControlBackgroundListLowBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid ColumnSpacing="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{x:Bind Name, Mode=OneWay}"
                                     PlaceholderText="Row name"
                                     TextChanged="Name_TextChanged"/>
                            <Button Grid.Column="1" Padding="6" Click="MoveUp_Click" ToolTipService.ToolTip="Move up">
                                <FontIcon Glyph="&#xE70E;" FontSize="12"/>
                            </Button>
                            <Button Grid.Column="2" Padding="6" Click="MoveDown_Click" ToolTipService.ToolTip="Move down">
                                <FontIcon Glyph="&#xE70D;" FontSize="12"/>
                            </Button>
                            <Button Grid.Column="3" Padding="6" Click="Delete_Click" ToolTipService.ToolTip="Delete row">
                                <FontIcon Glyph="&#xE74D;" FontSize="12"/>
                            </Button>
                        </Grid>

                        <AutoSuggestBox Grid.Row="1"
                                        Header="Right value"
                                        Tag="right"
                                        Text="{x:Bind RightText, Mode=OneWay}"
                                        PlaceholderText="Search sensor…"
                                        TextChanged="Slot_TextChanged"
                                        SuggestionChosen="Slot_SuggestionChosen"
                                        QuerySubmitted="Slot_QuerySubmitted"/>
                        <AutoSuggestBox Grid.Row="2"
                                        Header="Center value (temperature)"
                                        Tag="center"
                                        Text="{x:Bind CenterText, Mode=OneWay}"
                                        PlaceholderText="Search sensor…"
                                        TextChanged="Slot_TextChanged"
                                        SuggestionChosen="Slot_SuggestionChosen"
                                        QuerySubmitted="Slot_QuerySubmitted"/>
                        <AutoSuggestBox Grid.Row="3"
                                        Header="Bar (percent sensors)"
                                        Tag="bar"
                                        Text="{x:Bind BarText, Mode=OneWay}"
                                        PlaceholderText="Search sensor…"
                                        TextChanged="Slot_TextChanged"
                                        SuggestionChosen="Slot_SuggestionChosen"
                                        QuerySubmitted="Slot_QuerySubmitted"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="EmptyHint"
                   Grid.Row="2"
                   Text="No rows yet — use Add to create the first one."
                   Style="{StaticResource CaptionTextBlockStyle}"
                   Opacity="0.7"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</Page>
